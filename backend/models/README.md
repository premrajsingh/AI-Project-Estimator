# The effort_model.pkl is a large binary file (~44MB).
# To generate it, run the training script:
#   cd machine_learning
#   python train_model.py
# Or download it from your cloud storage / CI artifacts.
